# Core Flask Framework
Flask==2.3.3
Flask-CORS==4.0.0

# Data Processing
pandas==2.1.4
openpyxl==3.1.2

# Document Processing
python-docx==0.8.11

# Date/Time Processing
python-dateutil==2.8.2

# Environment Configuration
python-dotenv==1.0.0

# Password Hashing (Security Enhancement)
bcrypt==4.0.1

# Cross-platform Email Integration (SMTP)
# pywin32==306  # Removed for cross-platform compatibility

# Production Server
gunicorn==20.1.0

# Note: The following packages were removed as they are not used in the codebase:
# - transformers (AI/ML library - not used)
# - torch (Deep learning - not used) 
# - google-genai (Google AI - not used)
# - openai (OpenAI API - not used)
# - XlsxWriter (Excel writing - not used)
# - SQLAlchemy (ORM - not used, using raw SQLite)
# - flask_sqlalchemy (Flask ORM - not used)
# - flask_migrate (DB migrations - not used)
